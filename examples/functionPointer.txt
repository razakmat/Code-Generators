

typedef int (*ptr) (int);

int foo(int a)
{
    if (a == 0)
        return 0;
    if (a == 1)
        return 1;
    return foo(a - 1) + foo(a - 2);
}


void main() 
{
    int a;
    ptr f = &foo;

    a = f(8);

    print(a);   // 21

}
